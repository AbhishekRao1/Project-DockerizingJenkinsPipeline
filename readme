The repository contains a simple Java application which outputs the string "Hello world from Docker!" and is accompanied by a couple of unit tests to check that the main application works as expected.
This demonstrates the pipeline which runs on code commit and creates Docker image, pushes the image to Docker Hub and finlaly pull and run the image.